{
  "name": "dashie-ha-app",
  "version": "0.1.38",
  "description": "Dashie Home Assistant add-on — control center, voice pipeline broker, and HA metrics bridge.",
  "main": "server/index.js",
  "private": true,
  "scripts": {
    "start": "node server/index.js",
    "dev": "node --env-file-if-exists=.env server/index.js",
    "test:ha": "node --env-file-if-exists=.env scripts/test-ha.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0",
    "express": "^4.19.2",
    "ws": "^8.20.0"
  }
}
